import styled from 'styled-components';

export const StyledColon = styled.div`
  font-weight: ${({ theme }) => theme.antd.fontWeightStrong};
  font-size: ${({ theme }) => theme.antd.fontSizeHeading1}px;
`;

export const StyledDaysContainer = styled.div`
  margin-right: ${({ theme }) => theme.antd.marginLG}px;
`;
